# Hungry Point - Duraha UI

React + TypeScript + Tailwind frontend for the public cafe site, customer ordering, POS, kitchen display, and admin/employee management.

```bash
npm install
cp .env.example .env
npm run dev
```

Open `http://localhost:5173`.
